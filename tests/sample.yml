cache_dir: /
output_dir: /

timeout: 450
interval: 600

host: 0.0.0.0
port: 8888

# Not the real binary but it is known to exist
rpki_client: /bin/bash

trust_anchor_locators:
  # no semantic check is done - but files are checked for existence.
  # workaround for systems w/o tals
  - /etc/hosts
